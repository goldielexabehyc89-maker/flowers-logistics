/**
 * Коды ошибок приложения и русские сообщения для интерфейса.
 *
 * Сообщение для пользователя никогда не содержит технических деталей, секретов и PII.
 * Технические подробности уходят только в структурированный лог и администратору.
 */

export const ERROR_CODES = [
  'VALIDATION_FAILED',
  'UNAUTHENTICATED',
  'FORBIDDEN',
  'NOT_FOUND',
  'CONFLICT',
  'RATE_LIMITED',
  'INTERNAL_ERROR',
  'SERVICE_UNAVAILABLE',
] as const;

export type ErrorCode = (typeof ERROR_CODES)[number];

export const ERROR_MESSAGES: Record<ErrorCode, string> = {
  VALIDATION_FAILED: 'Проверьте правильность заполнения полей.',
  UNAUTHENTICATED: 'Требуется вход в систему.',
  FORBIDDEN: 'Недостаточно прав для выполнения операции.',
  NOT_FOUND: 'Запись не найдена.',
  CONFLICT: 'Данные были изменены другим пользователем. Обновите страницу.',
  RATE_LIMITED: 'Слишком много попыток. Повторите позже.',
  INTERNAL_ERROR: 'Внутренняя ошибка сервиса. Попробуйте позже.',
  SERVICE_UNAVAILABLE: 'Сервис временно недоступен.',
};

export const ERROR_STATUS: Record<ErrorCode, number> = {
  VALIDATION_FAILED: 400,
  UNAUTHENTICATED: 401,
  FORBIDDEN: 403,
  NOT_FOUND: 404,
  CONFLICT: 409,
  RATE_LIMITED: 429,
  INTERNAL_ERROR: 500,
  SERVICE_UNAVAILABLE: 503,
};

/**
 * Машинно-читаемые причины конфликта.
 *
 * Общий код `CONFLICT` отвечает на вопрос «почему 409» одинаково для всех случаев,
 * а интерфейсу нужно разное поведение: устаревшую версию достаточно перезапросить,
 * а заказ, уже лежащий в другом маршруте, нужно показать вместе с его номером.
 * Разбирать текст сообщения ради этого нельзя — он предназначен человеку.
 */
export const CONFLICT_KINDS = [
  /** Заказ уже состоит в другом активном маршруте. Возвращается его номер. */
  'ORDER_ALREADY_IN_ROUTE',
  /** Переданная версия записи устарела: кто-то изменил её раньше. */
  'STALE_VERSION',
  /** Заказ не пригоден для ручного распределения. */
  'ORDER_NOT_ELIGIBLE',
  /** Операция допустима только для черновика. */
  'ROUTE_NOT_DRAFT',
  /** Переданный порядок не совпадает с текущим активным составом маршрута. */
  'ROUTE_ORDER_SET_MISMATCH',
  /** Пустой маршрут подтверждать нечего. */
  'ROUTE_EMPTY',
  /** У заказов маршрута есть известные расхождения с МоимСкладом. */
  'ROUTE_HAS_CONFLICTS',
  /** Назначенный курьер больше не может выполнять маршрут. */
  'ROUTE_COURIER_UNAVAILABLE',
  /** Операция требует активной блокировки редактора. */
  'EDIT_LOCK_REQUIRED',
  /** Маршрут сейчас редактирует другой человек или другое устройство. */
  'EDIT_LOCK_HELD_BY_OTHER',
  /** Переданная версия блокировки устарела: её успели перехватить. */
  'EDIT_LOCK_STALE',
  /** Такой же расчёт матрицы уже выполняется другим экземпляром приложения. */
  'MATRIX_IN_PROGRESS',
  /** Склад по умолчанию не настроен: планировать не от чего. */
  'DEPOT_NOT_CONFIGURED',
  /**
   * У основного склада не определены координаты.
   *
   * Отдельная причина, а не разновидность «не настроен»: склад выбран,
   * и человеку надо не создавать новый, а выбрать его адрес из подсказок
   * повторно.
   */
  'DEPOT_POINT_MISSING',
  /** Склад по умолчанию нельзя выключить: сначала назначьте другой. */
  'DEPOT_DEFAULT_REQUIRED',
  /** Общая смена не сохранена администратором. Планирование без неё невозможно. */
  'SHIFT_NOT_CONFIGURED',
  /** У дня уже есть незавершённый запуск планирования: расчёт или готовое превью. */
  'PLAN_RUN_IN_PROGRESS',
  /** Операция допустима только для готового превью. */
  'PLAN_NOT_PREVIEW',
  /**
   * Вход плана устарел: заказы, геоданные, склад, смена или слоты изменились
   * с момента расчёта. Превью снято, черновики не созданы ни одного.
   */
  'PLAN_INPUT_STALE',
  /** В плане есть неразмещённые заказы: применение требует отдельного подтверждения. */
  'PLAN_HAS_UNASSIGNED',
  /** Ячейка с таким кодом уже существует. */
  'CELL_CODE_TAKEN',
  /** В ячейке есть заказы: тип меняется только у пустой. */
  'CELL_NOT_EMPTY',
  /**
   * Пустоту ячейки подтвердить нечем: модуль подключён без проверки занятости.
   * Считать неизвестное пустым нельзя — именно так заказы и уезжают не туда.
   */
  'CELL_OCCUPANCY_UNKNOWN',
  /** Номер заказа не однозначен: случайный заказ не выбирается. */
  'ORDER_NUMBER_AMBIGUOUS',
  /** Ячейка выключена: класть в неё заказ нельзя. */
  'CELL_INACTIVE',
  /** Отсканирована маршрутная ячейка при обычной приёмке: нужен явный выбор. */
  'ROUTE_CELL_REQUIRES_CHOICE',
  /** Тип ячейки не подходит операции. */
  'CELL_KIND_MISMATCH',
  /** Заказ размещает другой сотрудник прямо сейчас. */
  'PLACEMENT_RACE',
  /** Маршрут не подтверждён либо уже передан курьеру. */
  'ROUTE_NOT_CONFIRMED',
  /** У маршрутного листа уже есть активная маршрутная ячейка. */
  'ROUTE_CELL_ALREADY_BOUND',
  /** Маршрутная ячейка ещё не привязана. */
  'ROUTE_CELL_NOT_BOUND',
  /** Отсканирована не та маршрутная ячейка. */
  'ROUTE_CELL_MISMATCH',
  /** Заказ не входит в активный состав маршрута. */
  'ORDER_NOT_IN_ROUTE',
  /** Заказ помечен проблемным: комплектование и выдача заблокированы. */
  'ORDER_BLOCKED',
  /** Заказ не принят на склад: текущего размещения нет. */
  'ORDER_NOT_PLACED',
  /** Маршрут менялся после комплектования: заказ требует перемещения. */
  'PLACEMENT_REQUIRES_RELOCATION',
  /** Выдача уже начата. */
  'ISSUE_SESSION_OPEN',
  /** Выдача не начата: сначала подтвердите курьера. */
  'ISSUE_SESSION_REQUIRED',
  /**
   * По маршруту уже есть физически переданные курьеру заказы: обычное
   * редактирование, возврат в черновик и отмена ему недоступны.
   */
  'ROUTE_ALREADY_ISSUED',
  /** Без активной смены новый заказ взять нельзя. */
  'FLORIST_SHIFT_REQUIRED',
  /** Заказ уже взят другим флористом: захват проиграл гонку. */
  'ORDER_ALREADY_CLAIMED',
  /** Заказ непригоден для сборки: не та область, нет подтверждённого состава. */
  'ORDER_NOT_ASSEMBLABLE',
  /** Действие требует, чтобы заказ был закреплён именно за этим флористом. */
  'ORDER_NOT_ASSIGNED_TO_YOU',
  /** Заказ не в том состоянии процесса, которого требует действие. */
  'ORDER_PROCESS_STATE_MISMATCH',
  /** У флориста нет активной смены: назначать заказ ему нельзя. */
  'FLORIST_NOT_ON_SHIFT',
  /**
   * Назначение осталось от закрытой смены.
   *
   * Заказ не потерян: он ждёт решения администратора — переназначения активному
   * флористу либо возврата в общую очередь.
   */
  'ORDER_ASSIGNMENT_SHIFT_CLOSED',
  /** Задание печати уже завершено: повторная отметка ничего не меняет. */
  'PRINT_JOB_ALREADY_COMPLETED',
  /** Маршрут не выдан курьеру, отменён либо уже завершён. */
  'ROUTE_NOT_ACTIVE',
  /** У заказа уже есть действующий результат доставки. */
  'RESULT_ALREADY_RECORDED',
  /** Результат уже отменён: вторая отмена ничего не меняет. */
  'RESULT_ALREADY_CANCELLED',
  /**
   * Пять минут на самостоятельное исправление истекли.
   *
   * Это не отказ в праве, а смена исполнителя: дальше исправляет логист
   * или администратор, и уже с обязательной причиной.
   */
  'CANCEL_WINDOW_EXPIRED',
  /** Заказ доставляется курьером: выдавать его покупателю нельзя. */
  'ORDER_NOT_PICKUP',
  /** Самовывозный заказ уже выдан покупателю. */
  'PICKUP_ALREADY_ISSUED',
  /** У заказа нет локальной правки адреса: снимать нечего. */
  'ADDRESS_NOT_CORRECTED',
  /** У заказа нет расхождения адресов: решать нечего. */
  'ADDRESS_NO_CONFLICT',
  /** Ручная отгрузка выключена глобальной настройкой администратора. */
  'MANUAL_ISSUE_DISABLED',
  /** Отгрузить маршрут без назначенного курьера нельзя. */
  'ROUTE_COURIER_REQUIRED',
  /** Отменять нечего: маршрут не отгружен. */
  'ROUTE_NOT_ACTIVE',
] as const;

export type ConflictKind = (typeof CONFLICT_KINDS)[number];

/**
 * Подробности конфликта. Персональных данных не содержит:
 * только вид, номер маршрута и идентификаторы заказов.
 */
export interface ConflictDetails {
  kind: ConflictKind;
  routeNumber?: string;
  orderIds?: string[];
}

export interface ApiErrorBody {
  error: {
    code: ErrorCode;
    /** Сообщение на русском языке, пригодное для показа пользователю. */
    message: string;
    /** Идентификатор запроса для сопоставления с логом. */
    requestId?: string;
    /** Заполняется только для `CONFLICT`. */
    conflict?: ConflictDetails;
  };
}
