# Roadmap

Статусы: `Backlog`, `Design`, `Ready`, `In progress`, `Review`, `Staging`, `Done`, `Blocked`.
`Design` означает, что границы объёма согласованы владельцем и записаны, а реализация
ещё не начиналась.

Правила ведения:

- Разработчик обновляет только фактический статус уже утверждённой задачи.
- Новый продуктовый объём не добавляется, этапы не удаляются, приоритеты не меняются
  без решения владельца.
- Завершённый пункт содержит ссылку на commit/PR и реально пройденные проверки.
- `Done` ставится только после фактически выполненных обязательных проверок.

---

## Этап 1. Технический фундамент, авторизация, роли и UI shell

Поставляется четырьмя последовательными ветками. После каждой — отчёт и отдельное разрешение
владельца на merge; следующая ветка создаётся от обновлённой `main`. Пункт 1.5 добавлен
отдельным решением владельца: это инфраструктурная ветка без продуктового объёма.

### 1.1. Репозиторий, окружение, схема и первая миграция — `Done`

Выполнено: PR #1, squash-commit `ae0f1bc15d3d04a16169789fba2ffa6dffdc33af`.
Фактические проверки: 41 критический тест, `typecheck`, `lint`, `format`, production-сборка,
4 миграции на чистой PostgreSQL 16 без дрейфа, поиск секретов gitleaks — всё зелёное в CI.

<details>
<summary>Исходный объём пункта</summary>

Ветка `feat/stage1-foundation`.

Объём: структура модульного монолита, ADR, команды `dev/build/typecheck/lint/test/test:critical`,
`.editorconfig`, `.gitignore`, `.env.example`, `.nvmrc`, `.npmrc`, Docker Compose с PostgreSQL 16
и приложением, production-Dockerfile, health/readiness, структурированные логи с редакцией,
схема этапа 1 и forward-only миграции, заглушки границ модулей, CI и шаблон PR.

Критерии готовности: `typecheck`, `lint`, production `build`, чистое применение миграций
на новой PostgreSQL 16, критические тесты инвариантов БД, редакции логов и конфигурации.

Проверки: см. раздел «Проверки» в `README.md`; фактические результаты — в отчёте по ветке.

</details>

### 1.2. Авторизация и управление пользователями — `Done`

Выполнено: PR #2, squash-commit `b98084f59f11adb7252002ff5da7fce3f49d4e88`.
Фактические проверки в CI: 132 критических теста, `typecheck`, `lint`, `format`,
production-сборка, 4 миграции на чистой PostgreSQL 16 без дрейфа, `npm audit` без уязвимостей,
gitleaks без утечек.

Реализовано: bootstrap первого администратора с идемпотентностью и `--reissue`; активация
одноразовым кодом и установка PIN; вход по телефону и PIN; refresh-ротация с grace-окном
и обнаружением повторного использования; выход с устройства и со всех устройств; прогрессивная
блокировка перебора; role guards; управление пользователями и курьерами без физического
удаления; защита последнего активного администратора; неизменяемый аудит.

Локальные проверки выполнить не удалось: файловая система Docker на машине владельца
деградировала (диск 97%), поэтому по решению администратора обязательные проверки выполнялись
в чистом окружении GitHub Actions.

Объём: bootstrap первого администратора, вход по телефону и PIN, первый вход по временному коду
и установка PIN, logout устройства и отзыв всех сессий, refresh-ротация с обнаружением повторного
использования, rate limit и прогрессивная блокировка, role guards, управление пользователями
и курьерами (создание, просмотр, изменение, заморозка, разморозка, история — без физического
удаления), повторная выдача временного кода, reset PIN, аудит всех действий.

### 1.3. Общий UI-каркас — `Done`

Выполнено: PR #3, squash-commit `edb046a0149a3b26f71971e8714bac3451726e5b`.
Фактические проверки в CI: 154 критических теста, `typecheck`, `lint`, `format`,
production-сборка, 4 миграции на чистой PostgreSQL 16 без дрейфа, `npm audit` без уязвимостей,
gitleaks без утечек.

Ветка `feat/stage1-ui-shell`.

Объём: login и first-login, desktop sidebar/topbar и mobile navigation, вкладки с role-based
visibility, пустые/загрузочные/ошибочные состояния, таблица, фильтры, форма, модальное
подтверждение, toast, design tokens, страница пользователей и курьеров, административная заглушка
обязательных настроек, индикатор `Подключено`/`Нет связи`/`Интеграция не настроена`,
PWA manifest и service worker только для shell и статики.

### 1.4. Realtime, outbox и каркас деплоя — `Done`

Выполнено: PR #4, squash-commit `ca2d8222b78087dc8b9793834eb7974ff3a2d819`.
Фактические проверки в CI: 200 критических тестов, 3 браузерных сценария,
`typecheck`, `lint`, `format`, production-сборка, 4 миграции на чистой PostgreSQL 16
без дрейфа, миграции и запуск приложения из собранного production-образа,
`npm audit` без уязвимостей, gitleaks без утечек.

Ограничение, зафиксированное на этапе ветки 2, закрыто: глобальная фоновая очистка
просроченных `successorTokenEnc` реализована и покрыта критическим тестом.

Ветка `feat/stage1-realtime-outbox-deploy`.

Объём: SSE с heartbeat, переподключением и восстановлением пропущенных событий из `RealtimeEvent`
с фильтрацией по пользователю и ролям; транзакционный outbox с тестовым внутренним handler'ом,
retry и идемпотентностью; fail-closed команды `deploy-staging`/`deploy-production` с `DRY_RUN=1`;
Playwright smoke в зафиксированном образе.

### 1.5. Staging и production на одном сервере — `Done`

Выполнено: PR #5, squash-commit `846b8d8de9e0a91102eadcef780e3a6c425d71d8`.
Фактические проверки в CI: 215 критических тестов, 3 браузерных сценария, `typecheck`,
`lint`, `format`, production-сборка, 4 миграции на чистой PostgreSQL 16 без дрейфа,
миграции и запуск приложения из собранного production-образа, `npm audit` без уязвимостей,
gitleaks без утечек.

Ветка `chore/deploy-single-host`.

Решение владельца: оба окружения живут на одном физическом сервере. Одинаковый `SSH_HOST`
разрешён, но только при различающихся каталоге, Compose-проекте, внешнем порте, томе базы,
файле окружения и маркере; совпадение любого из них — отказ до обращения к серверу.
Подтверждение прохождения staging читается из staging-каталога, маркер production
проверяется отдельно.

Реальные конфигурации выкатки вынесены из Git в игнорируемый каталог `deploy/private/`;
в репозитории остались только шаблоны `deploy/*.conf.example`. Раньше заполнение
отслеживаемого файла делало рабочее дерево грязным, а выкатка запрещает грязное дерево —
команда блокировала сама себя.

Известное ограничение схемы: сбой сервера затрагивает оба окружения сразу. Разнесение
на два сервера не требует изменений в приложении — достаточно указать другой `SSH_HOST`
и отпечаток в конфигурации production.

### Зафиксированные правила этапа 1

- **Конфигурация выкатки живёт вне Git.** В репозитории — только `deploy/*.conf.example`.
  Реальные значения лежат в `deploy/private/`, который целиком в `.gitignore`; секреты
  приложения не попадают и туда — они находятся только на сервере.
- **Внешние outbox-обработчики работают с доставкой «минимум один раз».** Аренда сообщения
  и отметка `OutboxProcessedMessage` защищают от повторного вызова в штатном режиме,
  но не дают гарантии «ровно один раз» при падении процесса между внешним вызовом
  и фиксацией. Поэтому любой будущий запрос наружу — в первую очередь в МойСклад —
  обязан быть идемпотентным по `idempotencyKey`, и ключ должен передаваться во внешний
  запрос с самого начала, а не добавляться потом.

---

## Этап 2. Read-only исследование МоегоСклада — `Done`

Выполнено: PR #7. Squash-commit фиксируется в начале этапа 3.

**Решения владельца по итогам исследования утверждены** и обязательны для этапа 3:
фильтр заказов — склад «Москва 01 - Маленковская 14, к1 (Ласфлор)» **и** «Способ доставки»
= «Доставка», по стабильным UUID; статус сохраняется и отображается, но заказ не включает
и не исключает, во внутренние состояния не преобразуется и обратно не пишется; источники
полей — `deliveryPlannedMoment`, `shipmentAddress`, «Получатель» целиком, «Комментарий
по доставке», «Время доставки» без резервных источников; неполные данные не отбрасывают
заказ, а поднимают его в «Требует внимания»; наличные — только при «Наличные/карта на ТТ»,
сумма `max(0, sum − payedSum)` целыми минорными единицами; первоначальная загрузка — от
начала текущего дня минус 3 дня без верхней границы, пагинация по 100; синхронизация —
один `GET` каждые 30 секунд с overlap и дедупликацией по UUID; webhooks не создаются,
чужие подписки не трогаются; интеграция строго read-only.

Готовность к отгрузке с МоимСкладом **не связана** — это внутреннее состояние нашего
приложения. Оно возникает после завершения сборки флористом
(`docs/OWNER_DECISIONS.md`, `FUL-002`), а не отдельной ручной отметкой кладовщика.

**Область запросов различается.** Первоначальная загрузка фильтруется на стороне API
по складу, способу «Доставка» и нижней границе даты. Delta-запрос так фильтровать нельзя:
заказ, перенесённый на другой склад или переключённый с «Доставка», выпал бы из выборки
и остался бы незамеченным. Поэтому delta-проход получает все изменённые `customerorder`,
а принадлежность к нашей области определяется локально: вошедший заказ импортируется,
вышедший сохраняется в истории и исключается из активного планирования с записью в аудит
и уведомлением активных `ADMIN` и `LOGISTICIAN`, если он был распределён. Физическое
удаление документа опрос по `updated` вернуть не может — оно обнаруживается только
суточной контрольной сверкой.

Перед реализацией без угадывания определяются два значения: UUID склада (одним read-only
запросом с проверкой единственности) и единицы `sum`/`payedSum` (сверкой с карточкой
заказа). Отложены и не блокируют этап 3: точный маппинг статусов, обратная запись,
судьба «Отгрузки» и отдельный сервисный пользователь.

Полные таблицы — [`docs/MOYSKLAD_MAPPING.md`](docs/MOYSKLAD_MAPPING.md).

<details>
<summary>Ход исследования</summary>

Исследование выполнено на реальном аккаунте: 19 `GET`-запросов, ни одной записи, ответов
`429` не было. Результат — [`docs/MOYSKLAD_MAPPING.md`](docs/MOYSKLAD_MAPPING.md).

Установлено: сущность заказа `customerorder`; плановая дата — `deliveryPlannedMoment`;
признак доставки и самовывоза — атрибут «Способ доставки» со справочником; способ оплаты —
атрибут «Тип Оплаты»; связь с «Отгрузкой» через `customerOrder` и `demands`; 32 статуса
с UUID; 33 пользовательских атрибута; фактические лимиты 45 запросов на 3 секунды на весь
аккаунт; подписки `customerorder` существуют, но отключены; токен принадлежит пользователю
с полными правами администратора.

Контрольная выборка за 3–5 августа 2026 года подтвердила фильтр: из 579 заказов склада
под условие «Способ доставки» = «Доставка» подошли 336. В них нашлась несостыковка,
из-за которой статус не может подменять фильтр: три заказа с доставкой имели статус
«Самовывоз Завершен».

Read-only невозможно установить судьбу документа «Отгрузка» при отмене, возврате и повторной
доставке — нужен отдельный согласованный эксперимент.

</details>

## Этап 3. Синхронизация заказов

Поставляется тремя последовательными ветками. Разделение не косметическое: денежные поля
и неизменяемая история заказов требуют отдельной проверки до запуска реального импорта.

### 3.1. Модель данных, read-only клиент и mapper — `Done`

Выполнено: PR #9, squash-commit `9ece0118f164938e13cfce5d891980f2931aa9e2`.
Фактические проверки в CI: 284 критических теста, 6 миграций на чистой PostgreSQL 16
без дрейфа, production-образ, gitleaks и `npm audit` без замечаний.

<details>
<summary>Исходный объём пункта</summary>

Ветка `feat/stage3-moysklad-orders-core`.

Объём: модель заказа, неизменяемой истории версий и курсора интеграции; forward-only
миграция с запретом физического удаления заказа и правки ревизий; read-only клиент
МоегоСклада с последовательным лимитером; mapper с каноническим снимком, разбором
интервала и денежным контрактом. Фонового опроса, API списка заказов, UI и выкатки
в этой ветке нет.

Единица денег подтверждена сверкой ответа API с карточкой заказа: МойСклад отдаёт
копейки. Хранение — целые `BigInt`, на границе JSON — десятичные строки.

UUID склада «Москва 01 - Маленковская 14, к1 (Ласфлор)» определён одним read-only
запросом по точному названию, совпадение единственное; зафиксирован
в `docs/MOYSKLAD_MAPPING.md`.

</details>

### 3.2. Первоначальная загрузка и синхронизация — `Done`

Выполнено: PR #10, squash-commit `39dd3907f53a547006ffbf3d05bad65083e03f60`.

Фактические проверки: 345 критических тестов, 8 миграций на чистой PostgreSQL 16 без
дрейфа, production-образ, браузерный smoke, gitleaks и `npm audit` без замечаний. Три job
из четырёх подтверждены самим CI; браузерный smoke выполнен локально в идентичном
изолированном окружении по правилу 15 при подтверждённой аварии GitHub Actions.
Полный CI повторяется на `main` после восстановления GitHub, до этого deploy запрещён.

<details>
<summary>Исходный объём пункта</summary>

Ветка `feat/stage3-moysklad-sync`.

Объём: первоначальная загрузка с серверными фильтрами и пагинацией, delta polling каждые
30 секунд с overlap-окном, суточная контрольная сверка, вход и выход заказа из нашей
области, `IntegrationStatus`, realtime-события и API списка заказов.

**Окружения.** На момент ветки 3.2 рабочий токен МоегоСклада существовал только
в production; локальная разработка, GitHub CI и staging работали без токена, без единого
сетевого обращения к внешнему API, на фикстурах и поддельном HTTP. Конфигурация fail
closed: токен или включённая синхронизация вне production останавливают запуск приложения.

Позднее решением владельца `ENV-004` (11.08.2026) staging получил разрешение работать
с тем же токеном **строго на чтение**. Это утверждённый будущий контракт: изменение
конфигурации и HTTP-клиента — отдельные задания, и на текущей версии описанный здесь
fail closed действует без изменений. Для local и CI запрет остаётся абсолютным.

**Пагинация — 100, а не 1000.** Документированный предел `expand` — 100 элементов,
и клиент всегда запрашивает `expand=state`, поэтому больший размер страницы отвергается
собственной проверкой **до** сетевого обращения. Отказом API этот предел не подкреплён:
живое наблюдение `FLORIST-DISCOVERY-01` показало `200` при большей странице с молча
неразвёрнутыми связанными данными (`docs/MOYSKLAD_MAPPING.md` §13б).

**Ветка к МоемуСкладу не подключалась и никуда не выкатывалась.**

</details>

### 3.3. Экран «Сделки» и staging — `Done`

Выполнено: PR #12, squash-commit `dd7c1dcf8e544189a03da8a642a6a53abb9ba6d9`.

Фактические проверки: 379 критических тестов, 9 миграций на чистой PostgreSQL 16
без дрейфа, полный зелёный CI (обязательные проверки, production-образ, браузерный
smoke, поиск запрещённых файлов). Staging обновлён до этого же SHA, 9 миграций
применены, `/ready` отвечает `200`.

<details>
<summary>Исходный объём пункта</summary>

Ветка `feat/stage3-deals-ui`.

Объём: рабочий экран заказов с блоком «Требует внимания» наверху, поиском, выбором даты
и пагинацией; ручной локальный интервал логиста, который не затирается синхронизацией
и снимает только интервальные причины внимания; понятное состояние интеграции;
безопасный каркас одностороннего снимка заказов для staging.

Выкатка на staging в эту ветку не входит: она возможна только после полного зелёного CI
на актуальной `main`.

</details>

## Этап 4. Ручные маршруты, состояния и маршрутные листы — `In progress`

Решением владельца этап разделён на три последовательные ветки: модель и черновики,
подтверждение с блокировкой редактора, интерфейс маршрутов.

### 4.1. Модель маршрутов и ручные черновики — `Done`

Выполнено: PR #13, squash-commit `642a34a0df215bcba3d2faee21af1e40e49a16c1`.

Фактические проверки: 409 критических тестов, 10 миграций на чистой PostgreSQL 16
без дрейфа, полный зелёный CI (обязательные проверки, production-образ, браузерный
smoke, поиск запрещённых файлов и секретов).

<details>
<summary>Исходный объём пункта</summary>

Ветка `feat/stage4-manual-route-drafts`.

Объём: модель маршрута и участия заказа с историей, конфликты распределённого заказа,
серверные операции над черновиками (создание, добавление, возврат, перемещение,
порядок, курьер), инварианты PostgreSQL, аудит и realtime. Интерфейса, подтверждения,
отмены и мягкой блокировки редактора здесь нет.

Внутреннее состояние заказа не хранится отдельным полем: оно вычисляется из активного
участия и состояния маршрута, поэтому второго источника истины не существует.

</details>

### 4.2. Подтверждение, отмена и мягкая блокировка редактора — `Done`

Выполнено: PR #14, squash-commit `fb0a60e395c6e908903adef894cec2c263243045`.

Фактические проверки: 439 критических тестов, 11 миграций на чистой PostgreSQL 16
без дрейфа, полный зелёный CI (обязательные проверки, production-образ, браузерный
smoke, поиск запрещённых файлов и секретов).

<details>
<summary>Исходный объём пункта</summary>

Ветка `feat/stage4-route-lifecycle-lock`.

Объём: переходы `DRAFT → CONFIRMED`, `CONFIRMED → DRAFT` и отмена из обоих состояний;
неизменяемая история переходов с обязательной причиной там, где она нужна; повторная
проверка состава при подтверждении; мягкая lease-блокировка редактора с истечением,
heartbeat и безопасным перехватом; применение блокировки ко всем операциям
редактирования черновика; аудит и realtime.

Состояний `ACTIVE` и `COMPLETED`, отгрузки, готовности склада, вместимости, карт
и интерфейса здесь нет.

</details>

### 4.3. Интерфейс маршрутов — `Done`

Выполнено: PR #15, squash-commit `5345c3b759059686864133de5ae9e0b7745334ca`.

Фактические проверки: 467 критических тестов, 11 миграций на чистой PostgreSQL 16
без дрейфа, семь браузерных сценариев, полный зелёный CI. Staging обновлён до этого
же SHA: 11 миграций применены, `/ready` отвечает `200`.

<details>
<summary>Исходный объём пункта</summary>

Ветка `feat/stage4-routes-ui`.

Объём: рабочие экраны «Маршрутизация» и «Маршрутные листы» вместо заглушек, работа
с составом черновика (добавление, возврат, перенос, порядок, курьер), клиент мягкой
блокировки с сердцебиением и осознанным перехватом, жизненный цикл маршрута
в интерфейсе, печатная форма маршрутного листа через `@media print`.

Карт, расстояний, ETA, вместимости и отгрузки здесь нет: этих данных ещё не существует,
и рисовать их означало бы показывать выдуманные цифры.

</details>

## Этап 5. Карты, геокодирование и автоматическая маршрутизация — `In progress`

Стек выбран владельцем и зафиксирован в `docs/GEO_ARCHITECTURE.md`: MapLibre GL JS,
собственная подложка OSM/PMTiles, DaData для геокодирования (production и staging
с совпавшими маркерами, `GEO-004`), собственная
Valhalla для маршрутов и матриц, VROOM для оптимизации. OR-Tools остаётся резервом,
Яндекс и 2ГИС — возможные платные источники после пилота. Публичные серверы OSM
не используются. Координаты на staging синтетические; текстовые адреса заказов
после реализации `ENV-004` приходят туда собственной read-only синхронизацией
МоегоСклада — это не копирование production-координат и не разрешение DaData
на staging (`docs/GEO_ARCHITECTURE.md`, раздел «Приватность»).

Этап поделён на пять последовательных веток.

### 5.1. Модель геоданных, MapLibre и ручная точка — `Done`

Ветка `feat/stage5-geodata-map-foundation`, PR #16,
squash `58bfb505f808a22d88675e37772b5292b7724c19`.

Объём: состояние георазрешения заказа с инвариантами базы, неизменяемая история
геоданных, инвалидация точки при смене адреса, ручная установка точки логистом,
карта заказов дня на MapLibre с честным состоянием «Карта не настроена».
Внешних обращений, геокодирования, матриц и оптимизации здесь нет.

### 5.2. Production-геокодирование и очередь — `Done`

Ветка `feat/stage5-dadata-geocoding`, PR #17,
squash `98e7d057fdc4c2a3387465509b16e57e6f0ce00f`.

Объём: клиент стандартизации DaData, отдельная очередь `OrderGeocodeJob`
с арендой заданий и ограниченным backoff, разбор `qc_geo`, перевод заказа
в `NEEDS_REVIEW` при недостаточной точности и в `FAILED` после исчерпания
повторов, защита от устаревшего результата и разовое наполнение очереди.
Обращения к DaData возможны только в production и только при обоих ключах.

### 5.3. PMTiles, Valhalla и матрицы — `Done`

Ветка `feat/stage5-pmtiles-valhalla-matrices`, PR #18,
squash `ab3e0587b8fabd5fdaeff30a422ec79171cafdd7`.

Объём: собственная подложка PMTiles со стилем, спрайтами и шрифтами с нашего
origin; воспроизводимые инструменты сборки подложки и дорожного графа;
раздача артефактов с поддержкой диапазонных запросов и проверкой контрольных
сумм; адаптер Valhalla с профилями `auto` и `pedestrian`; внутренний сервис
матриц с кэшем, single-flight и TTL; синтетические точки staging.

Живых данных о пробках в собственном стеке нет: время считается по постоянным
скоростям дорожного графа, и результат помечен `trafficMode: STATIC`.

Установка на staging потребовала шести исправлений, каждое отдельной веткой:
доставка Compose-файла (#19), воспроизводимая сборка подложки (#20), команда
запуска маршрутизатора (#22), идентичность графа по содержимому вместо времени
файла (#23), проверка артефактов, переживающая обрыв SSH (#24), безопасный
контракт пути с пробелами в именах шрифтов (#25), разрешение адресов стиля
относительно самого стиля (#26) и подключение веб-воркера MapLibre (#27).
Закрыто после проверки на настоящем наборе: карта отображается, дороги, вода
и здания рисуются, диапазонные запросы к архиву работают, внешних
картографических обращений нет.

Подписей на карте сначала не было: данные для них в наборе лежали, а стиль
их не рисовал — в нём не было ни одного слоя типа `symbol`. Исправлено
отдельной задачей (PR #29, squash `f8113fa33e3eef247927ad5230bcdb823ca80ba2`):
стиль вынесен в общий модуль и получил названия населённых пунктов, названия
улиц вдоль дорог и номера домов с четырнадцатого зума; названия русские
с откатом на общее имя.

Пересборка набора для этого не потребовалась. Инструмент `restyle-basemap`
меняет только стиль и его запись в манифесте, сверяя контрольную сумму архива
до и после. На сервере рядом с прежним набором собран `20260806-r2`:
неизменившиеся 263 файла связаны жёсткими ссылками, переданы только новый
стиль и манифест, диск не вырос. Прежний каталог оставлен целым — откат
это одна строка конфигурации.

Проверено владельцем на staging: населённые пункты, улицы и номера домов
отображаются, здания, дороги и вода различимы, загружены латинские
и кириллические глифы, внешних картографических запросов нет.

### 5.4. VROOM и автоматические черновики — `Done`

Выполнено: PR #32, squash-commit `43321c0a34f7504f776f3fa5c9e3e5c7a49b1ad0`.
CI на этом squash-коммите завершился `4/4 success` (run `31470397373`): обязательные
проверки, production-образ, браузерный smoke, поиск запрещённых файлов и секретов.

Объём: склад как предметная сущность, общие настройки планирования, расчёт
маршрутов решателем VROOM, неизменяемое превью и автоматические черновики
по отдельному подтверждению.

Сделано:

- сущность `Depot` с инвариантом «не более одного склада по умолчанию, и он
  всегда активен»: уникальный индекс, два `CHECK` и выделенная advisory-блокировка
  `730206`. Смена умолчания принимает тождество и версию прежнего склада и версию
  нового; ноль обновлённых строк завершает транзакцию ошибкой. Склады не удаляются;
- общие настройки: смена обязательна и умолчания не имеет
  (`SHIFT_NOT_CONFIGURED`), время обслуживания — десять минут на ЗАКАЗ для обоих
  типов транспорта, редактируемо;
- планирование в две фазы: расчёт создаёт неизменяемое превью, черновики
  создаёт только отдельное подтверждение. Неразмещённые заказы требуют ещё
  одного, отдельного согласия;
- два неизменяемых снимка: вход — до матрицы и решателя, результат — при
  переходе в превью. Слоты машин после постановки неизменяемы;
- один незавершённый запуск на день (`activeDateKey`); готовое превью
  не вытесняется молча — нужно явное истечение либо `replacePreviewId`;
- аренда расчёта по серверному времени с сердцебиением, не больше трёх
  восстановлений после падения процесса; обычная ошибка не повторяется;
- контракт решателя: обе матрицы на профиль, целые секунды и метры с округлением
  вверх, любая `null` останавливает расчёт, только индексы без адресов
  и координат, проверка ответа как точного разбиения;
- версия решателя проверяется ПОВЕДЕНИЕМ — пробной задачей на `service_per_type`,
  — и при выкатке, и при старте приложения;
- VROOM в отдельной внутренней сети Compose без проброса портов, образ закреплён
  тегом и digest;
- миграция расширяющая: склады у существующих ручных маршрутов необязательны,
  обязательность требуется только у маршрута, созданного планированием.

Известные ограничения записаны в отчёте ветки.

Код слит в `main` через PR #32. Миграция `20260816090000_stage54_route_planning`
применена на staging; VROOM развёрнут и проверен поведенческой пробой
с пользовательской матрицей и `service_per_type`.

Пилот на реальном дне заказов, нагрузочная проверка и сравнение качества
в этот пункт не входят и выполненными не считаются — это пункт 5.5.

### Долг по надёжности проверок — `Done`

Закрыт: PR #39, squash-commit `3ada71930ebdaa14d1cc85c193e38362d76647c2`,
CI 4/4 (run `31506380027`).

Причина была подтверждена детерминированным воспроизведением на базовой `main`:
критические файлы пишут в одну базу и ничего за собой не удаляют, а
`lifecycle.critical.test.ts` оставлял на дате расчёта заказы без подтверждённой
точки. Порядок `lifecycle → planning` ронял сценарий «брошенная аренда
перехватывается не больше трёх раз», обратный порядок проходил.

Решение: файл, выбирающий заказы по дате, бронирует целый календарный месяц,
и никакой другой критический файл не вправе создать заказ с датой доставки
внутри чужого месяца. Договор проверяется механически — проверка обходит
исходники критических тестов и называет нарушителя. Утверждения сценария
не ослаблены: `planning.critical.test.ts` не изменён.

### 5.5. Пилот, нагрузка и решение по платным источникам — `Backlog`

Объём: пилот на обезличенных адресах, нагрузочная проверка, сравнение качества
и решение владельца по Яндексу и 2ГИС.

### 5.5. Перепроектирование логистики: единый раздел и рабочий экран «Сделки» — `Review`

Реализовано в ветке `feat/logistics-deals-redesign` (deploy не выполнялся):

- **Адрес заказа** получил два источника и одно рабочее значение. Локальная
  правка логиста сильнее исходного `shipmentAddress` до будущей обратной записи;
  исходное поле не перезаписывается. Расхождение источника с правкой даёт явный
  конфликт, блокирует маршрутизацию и решается человеком. История значений
  неизменяема, персональные данные не дублируются в общий аудит и realtime.
- **Серверные подсказки DaData** отдают только стандартизованный адрес,
  координаты и признак точности; ключи остаются на сервере. Живой сервис
  разрешён в production и на staging при совпавших маркерах, в остальных
  случаях отказ происходит до сети.
- **«Сделки»** — рабочий экран дня: список слева, карта справа, один серверный
  отбор на список, карту и «выбрать все». Порядок выбора равен порядку
  остановок; заказы «Требует внимания» и заказы черновиков выбрать нельзя.
- **Маршруты ровно из выбора**: один черновик в порядке `1..N` с безопасной
  гонкой и повтором, либо автоматический расчёт, замораживающий ровно
  выбранные заказы. Ни то, ни другое ничего не подтверждает.
- **Навигация**: один пункт «Логистика» с пятью вкладками, сворачиваемое меню,
  mobile overlay, прежние адреса ведут в новый эквивалент. Отдельная вкладка
  «Планирование» убрана из навигации, домен и история расчётов сохранены.

Ключи DaData на staging не устанавливались, backfill не выполнялся: интеграция
там по-прежнему `NOT_CONFIGURED` до отдельной задачи настройки.

## Этап 6. Производственный контур, выдача и мобильная работа курьера — `Backlog`

Зависит от этапов 3–5. Этап открывается **производственным контуром**: путём заказа
от импорта до передачи курьеру или покупателю. Прежний одиночный срез «готовность
заказа к отгрузке» отменён — он не описывал реальный процесс
(`docs/OWNER_DECISIONS.md`, `WH-001` → `FUL-001`).

Новый этап при этом **не создаётся**, и этапы 7–9 **не перенумеровываются**.

Порядок пунктов отражает зависимости, а не удобство: каждый следующий пункт
опирается на данные, которых до предыдущего просто нет. Ни один пункт ниже
не реализован: `Design` означает, что границы согласованы владельцем и записаны,
а кода ещё нет.

### 6.1. Роли, закрытые списки и производственная область — `Design`

Границы: `docs/OWNER_DECISIONS.md`, `FUL-001` и `FUL-005`.

Объём: роли `FLORIST` и `MANAGER` в модели ролей и правах; производственная область
заказов — утверждённый склад и **все** способы получения, включая самовывоз;
рабочие представления «Сегодня» и «Завтра». Логистическая область и признак
`inScope` не меняются: фильтр «Доставка» остаётся признаком логистических экранов
и маршрутизации и на самовывоз не расширяется.

### 6.2. Импорт состава, бандлов и производственных снимков — `Review`

Границы: `docs/OWNER_DECISIONS.md`, `FUL-005`.

Реализовано в ветке `feat/florist-order-content`: состав, компоненты комплектов
и неизменяемая история производственного снимка; очередь дозагрузки состава,
не зависящая от `customerorder.updated`; проектор показа сервисных позиций.
Экрана флориста, назначения, сборки, печати и фото-прокси пункт не содержит —
они относятся к 6.3.

Объём: импорт состава заказа (`positions`), состава бандлов и компонентов
в отдельные связанные сущности общей базы; неизменяемый снимок состава,
использованного при сборке, для сравнения после изменений. Фотографии в БД
не сохраняются: сервер получает их по защищённому каналу и проксирует клиенту.

Внешний контракт состава **подтверждён** живым read-only исследованием
`FLORIST-DISCOVERY-01`: позиции, их идентичность, количество, `assortment` и типы
`product`/`service`/`bundle`, а также компоненты бандла с собственной идентичностью
(`docs/MOYSKLAD_MAPPING.md` §13). Угадывать здесь больше нечего.

Обязательное требование пункта: импорт **fail closed** проверяет присутствие
и полноту состава. Наблюдение показало, что при странице больше 100 API отвечает
`200` и молча не разворачивает связанные данные — молчаливо принятый пустой
или усечённый состав недопустим.

Нижний комментарий заказа подтверждён как стандартное поле `description`
и сохраняется производственным снимком **отдельно** от логистического комментария
«Комментарий по доставке», контракт которого не меняется.

Политика сервисных позиций утверждена: скрываются только три известные доставки
по их стабильным UUID, «ЛФ-Выкладка Сердце» и любые новые неизвестные сервисные
позиции показываются, фильтр по типу и по названию запрещён. UUID четырёх позиций
подтверждены по активному и архивному справочнику и записаны
в `docs/MOYSKLAD_MAPPING.md` §13в как **конфигурация конкретного аккаунта**,
а не универсальные константы. Открытых вопросов по составу у пункта не осталось.

### 6.3. Назначение флориста, сборка, проверка изменений и очередь печати — `Design`

Границы: `docs/OWNER_DECISIONS.md`, `FUL-002`.

Объём: смена флориста; атомарное «Взять в работу» с эксклюзивным назначением;
списки «Общая очередь» и «Мои заказы» с сортировкой по срочности; карточка сборки;
действие «Собран»; независимое состояние печати (`ожидает`, `напечатано`, `ошибка`)
с повторной печатью; уведомление «Заказ изменён» до сборки и состояние «Требует
проверки» после неё; печатный бланк с номером заказа и QR, кодирующим этот же номер.

Открытым остаётся оборудование и механизм бездиалоговой печати (`FUL-007`):
очередь печати проектируется так, чтобы выбор принтера её не переписывал.

### 6.4. Ячейки и приёмка склада — `Design`

Границы: `docs/OWNER_DECISIONS.md`, `FUL-003` и `FUL-004`.

Объём: предметная модель ячеек (`STORAGE` и `ROUTE`, уникальный код, этикетка,
активность, деактивация вместо удаления, смена типа только у пустой); атомарная
приёмка парой сканов «заказ + ячейка»; неизменяемая история перемещений;
возврат заказа флористу на пересборку; очередь действий по отменённым заказам.

Логистический `Depot` этапа 5.4 не расширяется и не изменяется.

### 6.5. Комплектование подтверждённого маршрута — `Design`

Границы: `docs/OWNER_DECISIONS.md`, `FUL-003`.

Объём: поштучное возобновляемое перемещение заказов подтверждённого маршрута
в маршрутную ячейку; допустимость выдачи части заказов прямо из ячеек хранения;
состояние «Требуется перемещение» при изменении или отмене маршрута.

### 6.6. Выдача курьеру и перевод маршрута в `ACTIVE` — `Design`

Границы: `docs/OWNER_DECISIONS.md`, `FUL-003`.

Объём: подтверждение назначенного курьера, поштучное сканирование выдачи,
возобновляемая частичная выдача, признак «Выдан частично» у `CONFIRMED` маршрута,
перевод в `ACTIVE` после выдачи последнего заказа, освобождение маршрутной ячейки,
отмена выдачи администратором с обязательной причиной.

Здесь же неготовый заказ впервые становится блокирующим условием передачи.

### 6.7. Самовывоз и роль `MANAGER` — `Design`

Границы: `docs/OWNER_DECISIONS.md`, `FUL-001` и `FUL-003`.

Объём: отдельный раздел «Самовывоз» для `MANAGER` и `ADMIN`; поиск или скан номера
заказа, отображение фактической ячейки, фиксация «Выдан покупателю» без скана
ячейки и проверки получателя.

### 6.8. «Отгрузка» в МоемСкладе и выбранный механизм печати — `Backlog`

Объём: создание документа «Отгрузка» в МоемСкладе тем же серверным интеграционным
модулем с идемпотентностью и аудитом, и реализация выбранного механизма
бездиалоговой печати.

Пункт не начинается, пока владелец не определит момент и контракт создания
«Отгрузки» и не выберет оборудование печати (`FUL-007`). Любая операция записи
в МойСклад разрешается отдельным заданием (`FUL-006`).

### 6.9. Работа курьера: результаты доставки и завершение маршрута — `Review`

Реализовано в ветке `feat/courier-delivery-flow`: объединённый список выданных
`ACTIVE`-маршрутов курьера с группировкой и фильтром; результаты «Доставлен»
и «Не доставлен» с обязательной причиной из управляемого справочника; неизменяемая
попытка доставки со снимком названия причины; пятиминутная самостоятельная отмена
курьером и бессрочная корректировка `ADMIN`/`LOGISTICIAN` с причиной; атомарный
переход маршрута `ACTIVE → COMPLETED` после последнего результата и обратный
переход при отмене результата; история собственных результатов с маскированием
персональных данных со следующего московского дня.

Досрочность и опоздание показываются, но результат не блокируют: запрет закрыть
просроченный заказ потерял бы факт доставки.

### Остальной объём этапа 6

Передача заказов другому курьеру и **приём возврата товара от курьера** относятся
к последующей части этапа 6 и в пункты 6.1–6.9 не входят. Их порядок и разбиение
на ветки владельцем ещё не определены.

## Этап 7. Финансовый ledger, тарифы и отчёты — `Backlog`

Зависит от этапа 6. Объём: накопительный баланс курьера, неизменяемые операции, версии тарифов,
расходы и доплаты, операционные и финансовые отчёты, выгрузки Excel и PDF.

## Этап 8. Нагрузочная проверка, staging, backup/restore и приёмка — `Backlog`

Объём: проверка праздничной нагрузки, развёртывание staging, автоматические резервные копии
с обязательной проверкой восстановления, приёмка владельцем.

## Этап 9. Настройка серверов и разрешённый production-запуск — `In progress`

Сервер выбран и базово подготовлен владельцем: Ubuntu 24.04, Docker и Compose, UFW
с политикой «запрещено всё входящее, кроме 22/80/443», автоматические обновления,
вход только по ключу, root-вход и вход по паролю отключены. Реквизиты доступа
в репозитории не хранятся.

Staging настроен и **развёрнут** на этом сервере из коммита
`846b8d8de9e0a91102eadcef780e3a6c425d71d8`. Созданы каталог окружения, маркер, каталог
состояния, файл окружения с правами `600` и копия Compose из точного коммита `main`;
секреты сгенерированы на самом сервере и наружу не выводились. После выкатки проверено:
контейнеры `healthy`, четыре миграции применены, `/ready` = `200`, OCI-метка образа
совпадает с развёрнутым SHA, приложение слушает только петлевой интерфейс, SHA записан
в `verified-versions`. Доступ до настройки TLS — через SSH-туннель.

Staging **опубликован наружу по HTTPS** через обратный прокси Caddy: сертификат выдан
и продлевается автоматически, HTTP отвечает постоянным редиректом на HTTPS, приложение
по-прежнему слушает только петлевой интерфейс, firewall пропускает снаружи лишь SSH,
80 и 443. Переход ровно один, поэтому в окружении задано `TRUST_PROXY=1`. Проверено
снаружи: главная, вход, `/health`, `/ready` с `database=ok` и `/api/status` отвечают
`200`, защищённый API без токена — `401`.

Доступ к приватному пакету GHCR выдан отдельным credential только на чтение; широкий
токен GitHub на сервер не сохранялся.

При первой выкатке выявлен и исправлен дефект команды: путь `known_hosts` доходил
до `ssh` без внутренних кавычек, из-за чего путь с пробелами разбирался как список
файлов. Исправление закрыто регрессионным тестом.

Production не создавался и не запускался: каталога, файла окружения, тома, контейнеров
и конфигурации production не существует.

Первый администратор staging создан и активирован: вход по телефону и PIN работает.

Остаётся: production-запуск — только по отдельному распоряжению владельца.

---

## Вне roadmap реализации

**Товарный учёт.** Вне текущего roadmap остаются: остатки, приёмка поступлений
товара, перемещения запасов и списания. Источника этих данных в системе нет,
и решения владельца по ним не принималось.

**Импорт состава заказа (`positions`) больше не находится вне roadmap.** Он
включён в производственный контур пунктом 6.2 (`docs/OWNER_DECISIONS.md`,
`FUL-005`) и относится к составу конкретного заказа, а не к товарному учёту.
Точные внешние поля до отдельного read-only исследования не определены.

Складской модуль целиком вне roadmap не находится: производственный контур
включён в этап 6 пунктами 6.1–6.8. Роли `FLORIST` и `MANAGER` в модели ролей
ещё не существуют, а у роли `WAREHOUSE` пока нет ни разделов, ни прав: всё это
создаётся пунктом 6.1.
