-- Завершение смены больше не требует причины.
--
-- Прежнее правило было решением владельца и держалось инвариантом базы:
-- причина объясняла флористу, почему его смену закрыли за него. Владелец
-- это решение отменил: обязательное поле давало заполненную строку, а не
-- объяснение, и «ушёл домой» в журнале не сообщало ничего сверх самого
-- факта. Ответственность остаётся — кто и когда закрыл смену, видно
-- в аудите и в самой строке смены.
--
-- Прежние причины сохраняются: колонка остаётся, и записанное в ней —
-- по-прежнему история.
ALTER TABLE "FloristShift" DROP CONSTRAINT "FloristShift_forced_close_has_reason";
