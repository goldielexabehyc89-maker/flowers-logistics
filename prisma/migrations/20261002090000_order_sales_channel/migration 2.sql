-- Канал продаж заказа (UUID ссылки МоегоСклада).
--
-- Нужен, чтобы исключать заказы отдельных каналов (например, Flowwow) из
-- «Логистика → Сделки», не удаляя их из импорта и остальных потоков.
-- Nullable, без backfill: у существующих заказов значение появится при
-- следующей синхронизации, отсутствие канала — допустимое состояние.
ALTER TABLE "DeliveryOrder"
  ADD COLUMN "salesChannelId" UUID;
