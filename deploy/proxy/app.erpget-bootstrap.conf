# Install temporarily as /etc/caddy/app.erpget-bootstrap/certificate.conf.
# The folder contains a server-local certificate + private-key PEM bundle.
# force_automate in the site keeps automated management enabled alongside it.
load /var/lib/caddy/app.erpget-bootstrap
