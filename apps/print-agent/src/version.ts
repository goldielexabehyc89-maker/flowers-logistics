/**
 * Версия обработчика.
 *
 * Задана здесь, а не читается из манифеста рядом со сборкой. Тот манифест
 * существует ради одной строки — `"type": "module"` — и версии в нём нет
 * намеренно: два места, где написана версия, однажды разойдутся, и настройки
 * покажут не ту, что установлена.
 *
 * Версия попадает в реестр устройств при каждом обращении, поэтому пустая
 * или устаревшая графа означала бы неверный ответ на вопрос «что стоит на той
 * машине, которая не печатает» — ровно там, где он и задаётся.
 */
export const AGENT_VERSION = '0.1.0';
