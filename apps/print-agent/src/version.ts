/**
 * Версия обработчика.
 *
 * Задана здесь, а не читается из `package.json`: собранный `dist/` копируется
 * на рабочее место без манифеста, и чтение файла, которого рядом нет, оставило
 * бы в настройках пустую графу «версия» ровно у тех машин, о которых важнее
 * всего знать, что на них стоит.
 */
export const AGENT_VERSION = '0.1.0';
