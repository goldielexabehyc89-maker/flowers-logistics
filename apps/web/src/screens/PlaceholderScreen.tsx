/**
 * Заглушки будущих разделов.
 *
 * Заглушка обязана прямо говорить, что раздел не реализован, и на каком этапе
 * появится. Никаких выдуманных заказов, маршрутов и графиков: показанный
 * «почти работающий» экран создаёт ложное представление о готовности продукта.
 */

import { useAuth } from '../auth/AuthContext';
import { Button } from '../ui/components';

export interface PlaceholderProps {
  title: string;
  stage: string;
  description: string;
  upcoming: readonly string[];
}

export function PlaceholderScreen({
  title,
  stage,
  description,
  upcoming,
}: PlaceholderProps): React.JSX.Element {
  return (
    <section className="card stack">
      <div>
        <h2>{title}</h2>
        <p className="muted text-sm">Раздел ещё не реализован. Плановый этап: {stage}.</p>
      </div>

      <p>{description}</p>

      <div>
        <div className="field__label">Что появится в разделе</div>
        <ul className="text-sm muted">
          {upcoming.map((item) => (
            <li key={item}>{item}</li>
          ))}
        </ul>
      </div>

      <p className="text-sm muted">
        Данные не показываются намеренно: пока раздел не реализован, любые заказы, ячейки и счётчики
        здесь были бы выдуманными.
      </p>
    </section>
  );
}

export const PLACEHOLDERS: Record<string, PlaceholderProps> = {
  florist: {
    title: 'Флорист',
    stage: '6.2–6.3 — состав заказа и сборка',
    description:
      'Рабочий список заказов на сегодня и завтра, карточка сборки и завершение работы флористом.',
    upcoming: [
      'списки «Сегодня» и «Завтра» с сортировкой по срочности',
      'взятие заказа в работу и состав заказа с бандлами',
      'действие «Собран» и печатный бланк с QR-кодом номера заказа',
    ],
  },
  warehouse: {
    title: 'Склад',
    stage: '6.5 — приёмка и комплектование',
    description:
      'Приёмка собранных заказов в ячейки и комплектование подтверждённых маршрутных листов. ' +
      'Справочник ячеек уже заведён администратором в настройках; рабочих операций склада ещё нет.',
    upcoming: [
      'приёмка парой сканов «заказ + ячейка»',
      'перемещение заказов маршрута в маршрутную ячейку',
      'возврат заказа флористу на пересборку',
    ],
  },
  pickup: {
    title: 'Самовывоз',
    stage: '6.7 — выдача покупателю',
    description: 'Выдача самовывозных заказов покупателю по номеру заказа.',
    upcoming: [
      'поиск и сканирование номера заказа',
      'фактическая ячейка, в которой лежит заказ',
      'фиксация «Выдан покупателю»',
    ],
  },
  active: {
    title: 'Активные',
    stage: '6 — отгрузка и работа курьера',
    description: 'Отгруженные маршруты в работе и текущие результаты доставки.',
    upcoming: [
      'статусы «Доставлен» и «Не доставлен» с обязательной причиной',
      'передача заказов другому курьеру',
      'критические уведомления курьеру',
    ],
  },
  history: {
    title: 'История',
    stage: '6–7 — завершённые доставки и отчётность',
    description: 'Завершённые и отменённые заказы и маршруты с глобальным поиском.',
    upcoming: [
      'поиск заказа независимо от даты и статуса',
      'маскирование персональных данных курьера со следующего дня',
      'неизменяемая история операций',
    ],
  },
  reports: {
    title: 'Отчёты',
    stage: '7 — финансы и отчётность',
    description: 'Операционные и финансовые отчёты с выгрузками Excel и PDF.',
    upcoming: [
      'получено, распределено, отгружено, доставлено, опоздания',
      'баланс курьера, сдача и выдача денег, расходы',
      'аудит экспорта с персональными данными',
    ],
  },
};

/**
 * Экран для набора ролей, которому не соответствует ни один раздел.
 *
 * Роль здесь НЕ называется: прежняя версия объявляла такого пользователя
 * кладовщиком, и человек с любой другой ролью видел бы чужую подпись. Экран
 * говорит только о факте — доступных разделов нет, — и обязательно даёт выход:
 * без него нельзя завершить сессию на общем устройстве.
 *
 * Сейчас у каждой роли есть свой раздел, поэтому экран не должен появляться
 * в обычной работе. Он остаётся защитой от роли, заведённой раньше её раздела.
 */
export function NoSectionsScreen(): React.JSX.Element {
  const { user, logout, logoutEverywhere } = useAuth();

  return (
    <main className="shell__content">
      <section className="card stack">
        <h2>Доступных разделов нет</h2>
        <p>
          Для вашего набора ролей в приложении пока нет ни одного раздела. Это не ошибка входа:
          учётная запись активна, но работать в ней сейчас негде.
        </p>
        <p className="muted text-sm">
          Если вам нужен доступ к работе, попросите администратора добавить соответствующую роль.
        </p>

        <div>
          <div className="field__label">Вы вошли как</div>
          <div>{user?.fullName}</div>
          <div className="muted text-sm">{user?.phone}</div>
        </div>

        <div className="row">
          <Button variant="primary" onClick={() => void logout()}>
            Выйти
          </Button>
          <Button variant="ghost" onClick={() => void logoutEverywhere()}>
            Выйти на всех устройствах
          </Button>
        </div>
      </section>
    </main>
  );
}
